# Project Rules and Environment Constraints

## Environment
- **Operating System**: Windows
- **Shell**: PowerShell

## Command Line Rules
- **NEVER** use `cat`. Use `Get-Content` or simply rely on the `view_file` tool.
- **NEVER** use `&&` to chain commands. Use `;` in PowerShell.
- **NEVER** use `grep`. Use `Select-String` or the `grep_search` tool.
- **NEVER** use `ls`. Use `Dir` or the `list_dir` tool.
- **NEVER** use `export`. Use `$env:VAR = "value"`.
- Always assume PowerShell 5.1+ or PowerShell Core.

## AWS Specifics
- Always use the `--profile cloudAuditor` flag for AWS CLI commands in this project.
- Stack Name: `cloudauditor-dev`
- Region: `us-east-1`
